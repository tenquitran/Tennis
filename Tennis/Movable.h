#pragma once


namespace TennisApp
{
	// TODO: remove?
#if 0
	class IMovable
	{
	public:
		// Move the object to the specified distance (positive or negative).
		virtual void moveTo(int distX, int distY) = 0;
	};
#endif
}
